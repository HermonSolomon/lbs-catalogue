import React from "react";

const TabsWrapper = () => {
  return (
    <section className="asset-tabs">
      {/* Render <TabButtons /> */}

      {/* Render <TabContent /> */}
    </section>
  );
};

export default TabsWrapper;
